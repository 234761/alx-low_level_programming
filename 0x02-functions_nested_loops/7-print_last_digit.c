#include <stdio.h>

/**
 * main- Entry point
 *
 * Return: 0
 */
int print_last_digit(int)
{
	int nv;

	if (n > 0) 
	{
	nv = -1 * (nv % 10)
	_putchar(nv + '0');
	return (nv);
	}
	else 
	nv = (n % 10)
	_putchar(nv + '0');
	return (nv);
}
